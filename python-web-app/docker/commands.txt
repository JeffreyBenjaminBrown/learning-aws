docker run --name aws-py -it			\
  -v /home/jeff/code/aws/python-web-app/:/mnt	\
  -p 8000:8000 -d -h 127.0.0.1			\
  ofiscal/tax.co:new

# Start it as root; otherwise Apache won't run.
docker exec -it -u 0 aws-py bash
